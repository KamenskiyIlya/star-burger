# Сайт доставки еды Star Burger

Это сайт сети ресторанов Star Burger. Здесь можно заказать превосходные бургеры с доставкой на дом.

- [Ссылка на демонстрационный сайт](https://demonstrate-site-lesson.ru/)

![скриншот сайта](https://dvmn.org/filer/canonical/1594651635/686/)


Сеть Star Burger объединяет несколько ресторанов, действующих под единой франшизой. У всех ресторанов одинаковое меню и одинаковые цены. Просто выберите блюдо из меню на сайте и укажите место доставки. Мы сами найдём ближайший к вам ресторан, всё приготовим и привезём.

На сайте есть три независимых интерфейса. Первый — это публичная часть, где можно выбрать блюда из меню, и быстро оформить заказ без регистрации и SMS.

Второй интерфейс предназначен для менеджера. Здесь происходит обработка заказов. Менеджер видит поступившие новые заказы и первым делом созванивается с клиентом, чтобы подтвердить заказ. После оператор выбирает ближайший ресторан и передаёт туда заказ на исполнение. Там всё приготовят и сами доставят еду клиенту.

Третий интерфейс — это админка. Преимущественно им пользуются программисты при разработке сайта. Также сюда заходит менеджер, чтобы обновить меню ресторанов Star Burger.

## Быстрый запуск через Docker

Если у Вас установлен Docker или Docker desktop, используйте эту инструкцию, она проще и не требует ручной установки python, node js и postgreSQL. Все изменения, которые Вы будете вносить, автоматически будут подхватываться внутри контейнеров благодаря volume mounts:
- Код бэкенда (backend/) передается в /app — сервер Django перезагружается сам
- Код фронтенда (frontend/src/) передается в /app/src — Parcel автоматически пересобирает бандлы

Требования:
- [Docker](https://docs.docker.com/get-started/get-docker/)
- [Docker desktop](https://www.docker.com/products/docker-desktop/)

### Запуск

1. Клонируйте репозиторий и перейдите в директорию проекта:

```sh
git clone <url>
cd star-burger
```

2. Создайте файл переменных окружения .env и запишите в него обязательные переменные:
```
SECRET_KEY='ваш_секретный_ключ_django'
DEBUG=True # режим разработки
```

3. Запустите все сервисы через docker

```
docker compose up
```
> При первом запуске Docker соберет образы для бэкенда и фронтенда. Это может занять несколько минут.

4. Применяем миграцию БД в контейнере с помощью команды:
```sh
docker compose exec backend python manage.py migrate
```
5. Для доступа к панели администратора необходимо создать суперпользователя с помощью команды:
```sh
docker compose exec backend python manage.py createsuperuser
```

6. После того как все контейнеры будут успешно запущены, проведена миграция БД и настроено окружение Вы можете:

- Открыть сайт по адресу [http://localhost:8000](http://localhost:8000)
- Открыть панель администратора [http://localhost:8000/admin](http://localhost:8000/admin)

### Полезные команды

- Остановить контейнеры: `Ctrl+C` либо `docker compose down`
- Посмотреть логи определенного контейнера: `docker compose logs -f backend`
- Пересобрать образы после изменения Dockerfile: `docker compose build`

## Как запустить dev-версию сайта

Для запуска сайта нужно запустить **одновременно** бэкенд и фронтенд, в двух терминалах.

### Как собрать бэкенд

Сохраните к репозиторий на свое устройство в любое удобное место

Перейдите в каталог проекта:
```sh
cd star-burger
```

[Установите Python](https://www.python.org/), если этого ещё не сделали.

Проверьте, что `python` установлен и корректно настроен. Запустите его в командной строке:
```sh
python --version
```
**Важно!** Проект разрабатывался на версии Python 3.10.11, на других версиях могут быть проблемы с совместимостью.

Возможно, вместо команды `python` здесь и в остальных инструкциях этого README придётся использовать `python3`. Зависит это от операционной системы и от того, установлен ли у вас Python старой второй версии. 

В каталоге проекта создайте виртуальное окружение:
```sh
python -m venv venv
```
Активируйте его. На разных операционных системах это делается разными командами:

- Windows: `.\venv\Scripts\activate`
- MacOS/Linux: `source venv/bin/activate`


Установите зависимости в виртуальное окружение:
```sh
pip install -r requirements.txt
```

Создать файл `.env` в каталоге `star_burger/` и запишите туда:

```sh
SECRET_KEY='Секретный_ключ_django_проекта'
```

Создайте файл базы данных и отмигрируйте её следующей командой:

```sh
python manage.py migrate
```

Запустите сервер:

```sh
python manage.py runserver
```

Откройте сайт в браузере по адресу [http://127.0.0.1:8000/](http://127.0.0.1:8000/). Если вы увидели пустую белую страницу, то не пугайтесь, выдохните. Просто фронтенд пока ещё не собран. Переходите к следующему разделу README.

После того как будет собран фронтенд, Вам будут доступны:

- Главная страница (лендинг с меню и формой заказа): http://127.0.0.1:8000/
- Админка: http://127.0.0.1:8000/admin/
- Панель менеджера: http://127.0.0.1:8000/manager/orders/ . Доступ имеют только пользователи с is_staff=True.

#### Опционально

В проекте настроена возможность интеграции с Яндекс картами. По картам будут определяться координаты адресов клиентов и ресторанов, благодаря чему на странице менеджера будут отображаться - какой ресторан находится ближе всего к адресу клиента.

Зарегистрируйте API токен для работы с картами Яндекса(необходимы для получения координат) в [кабинете разработчика](https://developer.tech.yandex.ru/):

- Нажмите кнопку `Подключить API`
- Выберите `API Геокодер`
- Выберите `использование на бесплатной основе`
- Прочитайте и отметьте галкой, что согласны с условиями пользования
- Создайте API

Вам станет доступен api ключ от яндекса на 1000 бесплатных запросов в день.

<img width="1908" height="882" alt="howto" src="https://github.com/user-attachments/assets/d19490f6-9e95-49e4-8185-85bc0b9d9b90" />

- Добавьте в `.env` следующую переменную:

```sh
YANDEX_GEOCODER_API_KEY='Токен_от_yandex_api_из_предыдущего_шага'
```

### Собрать фронтенд

**Откройте новый терминал**. Для работы сайта в dev-режиме необходима одновременная работа сразу двух программ `runserver` и `parcel`. Каждая требует себе отдельного терминала. Чтобы не выключать `runserver` откройте для фронтенда новый терминал и все нижеследующие инструкции выполняйте там.

[Установите Node.js](https://nodejs.org/en/), если у вас его ещё нет.

Проверьте, что Node.js и его пакетный менеджер корректно установлены. Если всё исправно, то терминал выведет их версии:

```sh
nodejs --version
# v16.16.0
# Если ошибка, попробуйте node:
node --version
# v16.16.0

npm --version
# 8.11.0
```

Версия `nodejs` должна быть не младше `10.0` и не старше `16.16`. Лучше ставьте `16.16.0`, её мы тестировали. Версия `npm` не важна. Как обновить Node.js читайте в статье: [How to Update Node.js](https://phoenixnap.com/kb/update-node-js-version).

Перейдите в каталог проекта и установите пакеты Node.js:

```sh
cd star-burger
npm ci --dev
```

Команда `npm ci` создаст каталог `node_modules` и установит туда пакеты Node.js. Получится аналог виртуального окружения как для Python, но для Node.js.

Помимо прочего будет установлен [Parcel](https://parceljs.org/) — это упаковщик веб-приложений, похожий на [Webpack](https://webpack.js.org/). В отличии от Webpack он прост в использовании и совсем не требует настроек.

Теперь запустите сборку фронтенда и не выключайте. Parcel будет работать в фоне и следить за изменениями в JS-коде:

```sh
./node_modules/.bin/parcel watch bundles-src/index.js --dist-dir bundles --public-url="./"
```

Если вы на Windows, то вам нужна та же команда, только с другими слешами в путях:

```sh
.\node_modules\.bin\parcel watch bundles-src/index.js --dist-dir bundles --public-url="./"
```

Дождитесь завершения первичной сборки. Это вполне может занять 10 и более секунд. О готовности вы узнаете по сообщению в консоли:

```
✨  Built in 10.89s
```

Parcel будет следить за файлами в каталоге `bundles-src`. Сначала он прочитает содержимое `index.js` и узнает какие другие файлы он импортирует. Затем Parcel перейдёт в каждый из этих подключенных файлов и узнает что импортируют они. И так далее, пока не закончатся файлы. В итоге Parcel получит полный список зависимостей. Дальше он соберёт все эти сотни мелких файлов в большие бандлы `bundles/index.js` и `bundles/index.css`. Они полностью самодостаточны, и потому пригодны для запуска в браузере. Именно эти бандлы сервер отправит клиенту.

Теперь если зайти на страницу  [http://127.0.0.1:8000/](http://127.0.0.1:8000/), то вместо пустой страницы вы увидите:

<img width="1841" height="855" alt="image" src="https://github.com/user-attachments/assets/79879b56-4644-4231-8edd-9898f3b9f1b0" />

Каталог `bundles` в репозитории особенный — туда Parcel складывает результаты своей работы. Эта директория предназначена исключительно для результатов сборки фронтенда и потому исключёна из репозитория с помощью `.gitignore`.

**Сбросьте кэш браузера <kbd>Ctrl-F5</kbd>.** Браузер при любой возможности старается кэшировать файлы статики: CSS, картинки и js-код. Порой это приводит к странному поведению сайта, когда код уже давно изменился, но браузер этого не замечает и продолжает использовать старую закэшированную версию. В норме Parcel решает эту проблему самостоятельно. Он следит за пересборкой фронтенда и предупреждает JS-код в браузере о необходимости подтянуть свежий код. Но если вдруг что-то у вас идёт не так, то начните ремонт со сброса браузерного кэша, жмите <kbd>Ctrl-F5</kbd>.

## Настройка базы данных PostgreSQL

По умолчанию в проектах Django используется SQLite. В данном проекте используется более производительная СУБД - PostgreSQL. Ниже приведена инструкция по установке и настройке PostgreSQL на Ubuntu. Для других операционных систем шаги могут отличаться.

1. Установка PostgreSQL

Установите необходимые пакеты из официальных репозиториев Ubuntu:

```
sudo apt-get update
sudo apt-get install python3-pip python3-dev libpq-dev postgresql postgresql-contrib
```

2. Создание базы данных и пользователя

Когда мы устанавливали PostgreSQL, он автоматически создал пользователя `postgres`, у которого есть права администрировать PostgreSQL, нам необходимо изменить это имя для наших задач. Вводим в терминале:

```
sudo su - postgres
psql # входим в интерпретатор PostgreSQL
```

В интерпретаторе выполните следующие команды, заменив myproject, myprojectuser и password на ваши уникальные значения:

```
# создать БД
CREATE DATABASE myproject;

# создать пользователя с паролем
CREATE USER myprojectuser WITH PASSWORD 'password';

# настроить параметры подключения
ALTER ROLE myprojectuser SET client_encoding TO 'utf8';
ALTER ROLE myprojectuser SET default_transaction_isolation TO 'read committed';
ALTER ROLE myprojectuser SET timezone TO 'UTC';

# даем пользователю все права на БД
GRANT ALL PRIVILEGES ON DATABASE myproject TO myprojectuser;

# выходим из интерпретатора
\q

# выходим из под пользователя postgres
exit
```
3. Настройка переменного окружения

Вместо громоздких настроек в `settings.py`, добавьте в `.env` переменную `DB_URL` следующего формата:

```
DB_URL=postgres://myprojectuser:password@localhost:5432/myproject
```

где:
- myprojectuser - имя созданного пользователя БД
- password - пароль пользователя
- localhost - адрес сервера
- 5432 - стандартный порт PostgreSQL
- myproject - имя БД

В `settings.py` настройка БД должна выглядеть так:

```
DATABASES = {'default': env.dj_db_url('DB_URL')}
```
4. Миграция БД

```
python manage.py migrate
```

5. Запустите или перезапустите сайт и проверьте работоспособность БД.

> для переноса данных из существующих БД используйте dumpdata, loaddata.

## Как запустить prod-версию сайта

Собрать фронтенд:

```sh
./node_modules/.bin/parcel build bundles-src/index.js --dist-dir bundles --public-url="./"
```

Если Вы намерены запускать сайт в боевом режиме на сервере и пускать его в продакшн, предусмотрите вопросы безопасности.
Обязательно в переменном окружении настройте все указанные параметры, они по стандарту выставлены для возможности запустить проект локально. Без них запуск на сервере не безопасен. Откройте файл `.env` и определите следующие параметры:

- `DEBUG` — дебаг-режим. Поставьте `False`.
- `SECRET_KEY` — секретный ключ проекта. Он отвечает за шифрование на сайте. Например, им зашифрованы все пароли на вашем сайте.
- `ALLOWED_HOSTS` — [см. документацию Django](https://docs.djangoproject.com/en/5.2/ref/settings/#allowed-hosts).
- `CSRF_COOKIE_SECURE` - django проверяет, что cookie отправляются только через защищённое (HTTPS) соединение.
- `SESSION_COOKIE_SECURE` - сессионные куки будут передавать только по HTTPS.
- `SECURE_SSL_REDIRECT` - Принудительное перенаправление на HTTPS, на сайт не попасть через незащищенный запрос HTTP.
- `SECURE_HSTS_SECONDS` - настройка в Django, которая управляет использованием HTTP Strict Transport Security (HSTS). Она определяет длительность (в секундах), в течение которой браузер будет принудительно использовать HTTPS для доступа к сайту.
- `SECURE_HSTS_INCLUDE_SUBDOMAINS` - применяет HSTS ко всем поддоменам.
- `SECURE_HSTS_PRELOAD` - при значении True Django добавляет директиву preload в заголовок HSTS.

Перед выставлением этих настроек убедитесь, что понимаете, что каждая из них делает. Выставление некоторых настроек может привести к необратимым изменениям сайта.

Дополнительно проверьте себя нижеуказанной командой, django даст рекомендации по безопасности проекта:
```
python manage.py check --deploy
```

Также перед запуском сайта в боевом режиме, советую ознакомиться с рекомендациями по настройкам безопасности в [оф. документации django](https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/).

## Настройка логирования (Опционально)

К данному проекту предлагается использовать систему логирования [Rollbar](https://rollbar.com/). Она будет собирать все ошибки возникающие у проекта, если на сайте что-то перестанет работать Вы это не пропустите.

- Регистрируетесь и заходите в личный кабинет. На моменте выбора SDK выбирайте `Django`, вылезет инструкция, однако ниже будет расписана пошаговая настройка для данного проекта.
- На сайте Вам укажут токен от Вашей логгера, занесите этот ключ в `.env`:

```
ROLLBAR_TOKEN=ваш_токен
```
- Базово название среды `development`, это название будет отображаться рядом с ошибкой В ЛК Rollbar. Если Вас это не устраивает Вы можете поменять это прописав следующую переменную в `.env`:

```
ROLLBAR_ENVIRONMENT=production
```

В остальном Rollbar для проекта настроен и указанные в инструкции на сайте шаги делать не нужно. Рекомендуется проверить работоспособность системы создав тестовые view по примеру в инструкции на сайте:

- в `urls.py`:

```python
from foodcartapp import views

urlpatterns = [
    ...
    path('test_error/', views.test_error, name='test_error'),
]
```
- в `views.py`: 

```python
from django.http import HttpResponse

def test_error(request):
    x = 10 / 0
    return HttpResponse("This will not be reached")
```

- Запускаете сайте с помощью команды:

```
python manage.py runserver
```

- Переходите по ссылке `http://localhost:8000/test_error/`, должна вылезти ошибка ZeroDivisionError, а на Rollbar появиться отметка о появлении ошибки с названием окружения `ROLLBAR_ENVIRONMENT`.

## Быстрый деплой на сервер

У проекта предусмотрен скрипт для быстрого автоматического деплоя на сервер.
- Убедитесь, что в вашем аккаунте GitHub добавлен публичный SSH-ключ
- Убедитесь что вы включили SSH agent и добавили в него частный ssh ключ

1. Подключитесь к серверу

```
ssh пользователь@ip_сервера #либо просто кличку сервера, которую указали в ssh конфиге
```

2. Перейдите в директорию проекта

```
cd /opt/название_проекта
```

3. Запустите скрипт

```
./deploy_star_burger
```

Что сделает скрипт:
- Загрузит изменения из github
- Установит новые зависимости для python из `requirements.txt`
- Установит node.js зависимости
- Соберет фронтенд
- Соберет статику с помощью django команды `collectstatic`
- Сделает миграцию БД
- Перезапустит сервисы `nginx.service` и `star_burger.service`

Если все пройдет удачно, в конце скрипт сообщит об успешности процесса. Если на этапе деплоя произойдет ошибка, тогда скрипт прервется.

## Настройка оповещений о деплоях в Rollbar (опционально)

Rollbar позволяет отслеживать, какая версия кода находится на сервере, видеть данные о всех деплоях которые производились. Это помогает следить за версией проекта, а в случае возникновения ошибок быстро отследить с каким конкретно коммитом они связаны. Данная настройка не нужна на dev версии, полезна для production версии проекта.

1. Получите токен для деплоев

- Зайдите в Ваш проект Rollbar.
- Зайдите в раздел `Project Access Tokens`

<img width="500" alt="image" src="https://github.com/user-attachments/assets/8fb42332-5f5c-4f9b-8dd5-32ecbbfb24e6" />

- Создайте новый токен со Scope `post_server_item`:

<img width="500" alt="image" src="https://github.com/user-attachments/assets/8847e7de-fc92-4752-9a94-6e28c9a32284" />

- Скопируйте данный токен, т.к. он показывается один раз

2. Добавьте токен в `.env`

```
ROLLBAR_DEPLOY_TOKEN=ваш_токен
```
3. Подключите GitHub к Rollbar

- Вы также должны находиться в Вашем проекте
- Перейдите в раздел `Source control`

<img width="500" alt="image" src="https://github.com/user-attachments/assets/34f8ea41-7605-40f3-bf47-88b8a431c77c" />

- Авторизуйтесь в github
- Выберите нужный репозиторий и ветку
- Сохраните изменения

4. Проверьте работоспособность

- Запустите тестовый деплой на сервере, после успешного деплоя на `dashboard` в разделе `deploys` и `vesions` должна появиться информация о новом деплое.

<img width="500" alt="image" src="https://github.com/user-attachments/assets/4ca7e3b4-2cab-4ae0-8e0c-b0c3fdcb94b6" />

- В этом же разделе должны будут отображается новые коммиты, которые ещё не задеплоены на сервер

## Структура переменных окружения

В файле `.env` могут быть указаны следующие переменные:

Обязательные:
- `SECRET_KEY` - секретный ключ Django.
- `DEBUG` - режим отладки, для разработки `True`, для продакшена `False`
- `DB_URL` - URL для подключения к базе данных (В данной инструкции для PostgreSQL)

Опциональные:
- `ALLOWED_HOSTS` - список хостов, которым разрешается подключение к проекту, по умолчанию локальные адреса `127.0.0.1`, `localhost`
- `ROLLBAR_TOKEN` - токен для подключения к Rollbar (нужен только для продакшена)
- `ROLLBAR_ENVIRONMENT` - для удобства отладки на продакшене можно давать названия рабочим средам, по умолчанию стоит значение `development`, для продакшена следуют дать подобное название `production`
- `YANDEX_GEOCODER_API_KEY` - API-ключ для геокодера Яндекса (требуется для работы карт)

## Цели проекта

Проект создан в учебных целях. За основу был взят код проекта [FoodCart](https://github.com/Saibharath79/FoodCart).

